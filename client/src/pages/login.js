import { Button, Table } from "antd/lib";

const Login = () => {
  return (
    <div>
          <Button>Login</Button>
          
          Antd library is working now.

          now we ll connect backend and frontend 
    </div>
  );
};

export default Login;
